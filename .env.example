# chexi-trading root environment template
#
# Copy this file to `.env` in the repository root. This is the only env file for
# local development: Docker Compose, the Go API (godotenv), and Vite (apps/shadcn-admin)
# all read from here.
#
# Optional: force the API to load a specific file (overrides automatic root detection)
# CHEXI_ENV_FILE=/absolute/path/.env

# =============================================================================
# Frontend (Vite) — import.meta.env.VITE_*
# =============================================================================
# Used by apps/shadcn-admin (envDir points at this directory).
VITE_CLERK_PUBLISHABLE_KEY=
# Optional Go API base (no trailing slash). Omit or leave empty for same-origin `/api`
# (Vite dev proxy locally; Docker `chexi-web` nginx proxies `/api` → `chexi-api`).
# Do not set to http://chexi-api:8080 — that hostname exists only inside Docker, not in the browser.
# VITE_GOAPI_BASE_URL=http://localhost:8080

# =============================================================================
# Local application environment
# =============================================================================
APP_ENV=development
PORT=8080
APP_PUBLIC_URL=http://api.localhost

# =============================================================================
# Logging and HTTP server (optional API tuning)
# =============================================================================
# LOG_LEVEL=info
# HTTP_READ_HEADER_TIMEOUT_SEC=5
# HTTP_READ_TIMEOUT_SEC=30
# HTTP_WRITE_TIMEOUT_SEC=30
# HTTP_IDLE_TIMEOUT_SEC=120

# =============================================================================
# OpenAPI / Swagger (optional)
# =============================================================================
# SWAGGER_HOST=api.localhost
# SWAGGER_ENABLED=true

# =============================================================================
# PostgreSQL
# =============================================================================
# For Docker: POSTGRES_PASSWORD (db init) and DB_PASS (API) must match. The dev
# overlay syncs them when only one is set. If you change passwords after the DB
# volume exists, run `make docker-down-volumes` or update Postgres manually.
# Host-run API uses DB_HOST=localhost. Docker Compose maps the API container to
# the `chexi-db` service with DOCKER_DB_HOST below.
POSTGRES_USER=chexi_dev
POSTGRES_PASSWORD=change_this_local_postgres_password
POSTGRES_DB=chexi
DB_USER=chexi_dev
DB_PASS=change_this_local_postgres_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=chexi
DB_SSLMODE=disable
DOCKER_DB_HOST=chexi-db

# Publish DB only on loopback by default.
# POSTGRES_PUBLISH_HOST=127.0.0.1
# POSTGRES_PUBLISH_PORT=5432

# =============================================================================
# Redis
# =============================================================================
# Host-run API uses REDIS_HOST=localhost. Docker Compose maps the API container
# to the `chexi-redis` service with DOCKER_REDIS_HOST below.
REDIS_ENABLED=true
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_TLS_ENABLED=false
REDIS_TLS_SERVER_NAME=
REDIS_TLS_CA_CERT=
REDIS_TLS_INSECURE_SKIP_VERIFY=false
DOCKER_REDIS_HOST=chexi-redis

# REDIS_PUBLISH_HOST=127.0.0.1
# REDIS_PUBLISH_PORT=6379

# =============================================================================
# Admin web container (Docker Compose service chexi-web)
# =============================================================================
# VITE_* for the admin bundle are passed as Docker build args from this file when you
# `docker compose build`. By default HTTP is via Traefik only (no host publish for chexi-web).
# Optional: add infra/docker/docker-compose.direct-http.yml and set WEB_PUBLISH_* to map :5174.
# WEB_PUBLISH_HOST=127.0.0.1
# WEB_PUBLISH_PORT=5174

# =============================================================================
# API direct publish (optional; use docker-compose.direct-http.yml)
# =============================================================================
# API_PUBLISH_HOST=127.0.0.1
# API_PUBLISH_PORT=8080

# =============================================================================
# Docker Compose project name
# =============================================================================
# COMPOSE_PROJECT_NAME=chexi-trading

# =============================================================================
# Traefik (default with dev overlay: chexi-traefik in docker-compose.dev.yml)
# =============================================================================
# TRAEFIK_PUBLISH_HOST=127.0.0.1
# TRAEFIK_HTTP_PORT=80
# TRAEFIK_DASHBOARD_PORT=9081
# Add to /etc/hosts: api.localhost admin.localhost web.localhost pgadmin.localhost redis.localhost
# If port 80 is taken, set TRAEFIK_HTTP_PORT=9080 (or another free port) and use http://api.localhost:9080, etc.

# =============================================================================
# Auth and security
# =============================================================================
JWT_SECRET=replace-with-long-random-secret-at-least-32-chars
# MFA_ENCRYPTION_KEY=your-32-byte-key-base64-or-hex
# WEBHOOK_ENCRYPTION_KEY=your-32-byte-key-base64-or-hex
# PROVIDER_CREDENTIAL_ENCRYPTION_KEY=your-32-byte-key-base64-or-hex

# Login/register JSON shape (optional; unset = false)
AUTH_RESPONSE_INCLUDE_API_KEY=false
AUTH_RESPONSE_INCLUDE_USER=false

# JWT lifetimes (optional)
# JWT_EXPIRATION_DAYS=1
# JWT_ACCESS_TOKEN_MINUTES=15
# JWT_REFRESH_EXPIRATION_HOURS=720

# HTTP security headers (optional)
# SECURITY_HEADERS_ENABLED=true
# HSTS_ENABLED=false
# HSTS_MAX_AGE_SECONDS=31536000
# HSTS_INCLUDE_SUBDOMAINS=false

# =============================================================================
# Rate limiting and telemetry
# =============================================================================
# RATE_LIMIT_REQUESTS_PER_MINUTE=60
# RATE_LIMIT_BURST_SIZE=10
# RATE_LIMIT_REDIS_FAILURE_MODE=local_fallback
# AUDIT_HTTP_ENABLED=true
# AUDIT_HTTP_MUTATING_ONLY=true
# EVENT_TELEMETRY_RATE_LIMIT_RPM=120
# EVENT_TELEMETRY_BURST_SIZE=30

# =============================================================================
# Migrations and queues
# =============================================================================
# USE_VERSIONED_MIGRATIONS=false
# MIGRATIONS_DIR=migrations
# QUEUE_ENABLED=true
# QUEUE_ASYNC_ENABLED=true
# JOB_QUEUE_ENABLED=
# JOB_WORKER_ENABLED=true
# JOB_MAX_ATTEMPTS=
# JOB_RETRY_DELAY_SECONDS=
# QUEUE_WORKERS=2
# QUEUE_POLL_INTERVAL_MS=500
# QUEUE_MAX_ATTEMPTS=5
# QUEUE_INITIAL_BACKOFF_MS=1000
# QUEUE_MAX_BACKOFF_MS=300000
# QUEUE_SHUTDOWN_TIMEOUT_SEC=30
# QUEUE_DEAD_LETTER_MAX_CAP=10000

# =============================================================================
# OAuth and email
# =============================================================================
# OAUTH_GOOGLE_ENABLED=false
# OAUTH_GITHUB_ENABLED=false
# OAUTH_GOOGLE_CLIENT_ID=
# OAUTH_GOOGLE_CLIENT_SECRET=
# OAUTH_GITHUB_CLIENT_ID=
# OAUTH_GITHUB_CLIENT_SECRET=
# OAUTH_REDIRECT_BASE_URL=http://api.localhost
# OAUTH_STATE_TTL_MINUTES=10
# OAUTH_EXCHANGE_TTL_MINUTES=10

# EMAIL_ENABLED=true
# EMAIL_DRIVER=log
# EMAIL_PROVIDER=log # deprecated alias; EMAIL_DRIVER wins when both are set
# EMAIL_LOG_INCLUDE_BODY=false # unsafe dev/test-only opt-in; forbidden in staging/production
# EMAIL_FROM=noreply@localhost
# EMAIL_VERIFICATION_TTL_HOURS=48
# EMAIL_PASSWORD_RESET_TTL_HOURS=1
# ORG_INVITATION_TTL_HOURS=168
# EMAIL_RESEND_MIN_INTERVAL_SECONDS=60
# RESEND_API_KEY= # only required when intentionally setting EMAIL_DRIVER=resend
# EMAIL_REDIRECT_ALL_TO=

# =============================================================================
# Dev-only admin tools (reachable via Traefik: pgadmin.localhost / redis.localhost)
# =============================================================================
REDIS_COMMANDER_HTTP_USER=chexi
REDIS_COMMANDER_HTTP_PASSWORD=change_this_redis_commander_password
# Host port publish vars below are unused: dev overlay routes these UIs only through Traefik.
# REDIS_COMMANDER_PORT=8081

PGADMIN_DEFAULT_EMAIL=pgadmin-dev@example.com
PGADMIN_DEFAULT_PASSWORD=change_this_pgadmin_password
# PGADMIN_PUBLISH_PORT=5050

# =============================================================================
# Optional one-time first admin
# =============================================================================
# BOOTSTRAP_ADMIN_ENABLED=false
# BOOTSTRAP_ADMIN_EMAIL=admin@yourcompany.com
# BOOTSTRAP_ADMIN_PASSWORD=use-a-strong-unique-secret-not-this-one

# =============================================================================
# Coinbase Exchange public WebSocket (optional market data; off by default)
# =============================================================================
# Public feed only — no API keys used for WS. For reliable local tickers, prefer
# COINBASE_WS_ENVIRONMENT=production (sandbox liquidity/product coverage differs).
#
# Optional live adapter test (outbound WSS to Coinbase):
#   INTEGRATION_COINBASE_WS=1 go test -tags=integration -count=1 ./internal/marketdata/coinbase/...
# COINBASE_WS_ENABLED=false
# COINBASE_WS_PRODUCTS=BTC-USD,ETH-USD,SOL-USD
# COINBASE_WS_ENVIRONMENT=production
# COINBASE_WS_URL=
# COINBASE_WS_URL_SANDBOX=wss://ws-feed-public.sandbox.exchange.coinbase.com
# COINBASE_WS_URL_PRODUCTION=wss://ws-feed.exchange.coinbase.com
